@{
    Layout = null;
    var nombre = Context.Session.GetString("usuario") ?? "Jugador";
}
<style>
    body {
        margin: 0;
        background: #000;
        color: #00ffc3;
        font-family: 'JetBrains Mono', monospace;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
    }

    .alarma-container {
        text-align: center;
    }

    .alarma-container h2 {
        font-size: 24px;
    }

    .drag-area {
        position: relative;
        width: 300px;
        height: 100px;
        margin: 40px auto;
    }

    #drag {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: #00ffc3;
        position: absolute;
        top: 20px;
        left: 0;
        cursor: grab;
    }

    #drop {
        width: 60px;
        height: 60px;
        border-radius: 50%;
        background: #444;
        position: absolute;
        top: 20px;
        right: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
    }
</style>

<div class="alarma-container">
    <h2>🔔 ALARMA ACTIVADA 🔔</h2>
    <p>@nombre, despertate. ¡Tenés que llegar a la entrevista!</p>
    <p>Arrastrá el círculo al icono de silenciar para apagarla:</p>

    <div class="drag-area">
        <div id="drag" draggable="true"></div>
        <div id="drop">🔇</div>
    </div>
</div>

<script>
    const drag = document.getElementById("drag");
    const drop = document.getElementById("drop");

    drag.addEventListener("dragstart", (e) => {
        e.dataTransfer.setData("text/plain", "drag");
    });

    drop.addEventListener("dragover", (e) => e.preventDefault());

    drop.addEventListener("drop", (e) => {
        e.preventDefault();
        window.location.href = '/Home/SetHabitacion/1';
    });
</script>

