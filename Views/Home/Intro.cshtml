@{
    Layout = null;
}
<style>
    body {
        margin: 0;
        background: black;
        font-family: 'Segoe UI', sans-serif;
        color: white;
        overflow: hidden;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
    }

    .intro-text {
        text-align: center;
        font-size: 24px;
        padding: 30px;
        animation: fadeout 2s ease 6s forwards;
    }

    @@keyframes fadeout {
        to {
            opacity: 0;
            visibility: hidden;
        }
    }

    .alarma-container {
        display: none;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .reloj {
        font-size: 64px;
        color: #ff3c3c;
        font-family: 'Courier New', monospace;
        margin-bottom: 20px;
        animation: vibrar 0.1s infinite;
    }

    @@keyframes vibrar {
        0% { transform: translate(0px, 0px); }
        25% { transform: translate(1px, -1px); }
        50% { transform: translate(-1px, 1px); }
        75% { transform: translate(1px, 1px); }
        100% { transform: translate(-1px, -1px); }
    }

    #boton-apagar {
        padding: 10px 20px;
        font-size: 16px;
        background-color: #ff3c3c;
        border: none;
        color: white;
        cursor: pointer;
        border-radius: 5px;
    }

    #global-timer {
        position: fixed;
        top: 10px;
        left: 10px;
        font-size: 20px;
        font-family: 'Courier New', monospace;
        color: #ff3c3c;
        z-index: 9999;
        background: black;
        padding: 5px 10px;
        border-radius: 5px;
        display: none;
    }

    #tiempo-agotado {
        position: fixed;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        color: #ff3c3c;
        font-size: 40px;
        font-weight: bold;
        font-family: 'Courier New', monospace;
        background: black;
        padding: 20px;
        border: 3px solid red;
        display: none;
        z-index: 10000;
    }
</style>

<div class="intro-text" id="intro">
    Una mañana cualquiera... recibís una notificación. El Director Auraman quiere entrevistarte. La entrevista es a las 09:00. Te despertás a las 08:00. Pero... no podés salir de tu casa.
</div>

<div class="alarma-container" id="alarma">
    <div class="reloj" id="reloj-digital">08:00</div>
    <form method="post" action="/home/startgame">
    <button id="boton-apagar" type="submit">Apagar alarma</button>
</form>

</div>

<div id="global-timer"></div>
<div id="tiempo-agotado">TIEMPO AGOTADO</div>

<script>
    setTimeout(() => {
        document.getElementById("intro").style.display = "none";
        document.getElementById("alarma").style.display = "flex";
        document.getElementById("global-timer").style.display = "block";
        iniciarReloj();
    }, 7000);

    function iniciarReloj() {
        const startTime = new Date();
        startTime.setHours(8, 0, 0, 0);
        const limite = new Date(startTime.getTime() + 60 * 60 * 1000);

        const reloj = document.getElementById("reloj-digital");
        const global = document.getElementById("global-timer");

        function actualizar() {
            const ahora = new Date();
            const elapsedMs = ahora - startTime;

            if (elapsedMs < 0) {
                reloj.innerText = "08:00";
                global.innerText = "08:00:00";
                return;
            }

            const totalSec = Math.floor(elapsedMs / 1000);
            const horas = String(Math.floor(totalSec / 3600) + 8).padStart(2, '0');
            const minutos = String(Math.floor((totalSec % 3600) / 60)).padStart(2, '0');
            const segundos = String(totalSec % 60).padStart(2, '0');

            reloj.innerText = `${horas}:${minutos}`;
            global.innerText = `${horas}:${minutos}:${segundos}`;

            if (ahora >= limite) {
                document.getElementById("tiempo-agotado").style.display = "block";
                clearInterval(intervalo);
            }
        }

        actualizar();
        const intervalo = setInterval(actualizar, 1000);
    }
</script>
